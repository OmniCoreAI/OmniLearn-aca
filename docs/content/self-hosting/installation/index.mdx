import { Callout } from 'nextra/components'

# Installation Overview

The **OmniLearn CLI** is the recommended and officially supported way to install and manage self-hosted OmniLearn instances. The CLI automates the entire deployment process, so you can go from a fresh server to a running OmniLearn instance in minutes.

## What the CLI Does

The OmniLearn CLI handles:

- **Interactive setup wizard** that walks you through initial configuration
- **Docker Compose generation** based on your setup choices
- **Environment file generation** with all required variables pre-configured
- **Service management** to start, stop, and restart your instance
- **Health checks** to verify all services are running correctly
- **Backups and restores** for your database and uploaded content
- **Updates** to pull the latest OmniLearn images and apply them

<Callout type="info">
You do not need to manually create or edit Docker Compose files. The CLI generates and manages them for you based on your configuration choices.
</Callout>

## Next Steps

- [Requirements](/self-hosting/installation/requirements) -- Verify your server meets the minimum system requirements.
- [Quick Install](/self-hosting/installation/quick-install) -- Install the CLI and set up OmniLearn in minutes.
- [CLI Reference](/self-hosting/installation/cli-reference) -- Full reference of all available CLI commands.
